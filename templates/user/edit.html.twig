{% extends 'base.html.twig' %}

{% block title %}Modication profil{% endblock %}

{% block body %}

    <h1>Modifier le profil</h1>
    {{ form_start(userForm) }}
    {{ form_widget(userForm) }}
    <button class="ui primary button" type="submit">Enregistrer</button>
    {{ form_end(userForm) }}


    <br><br>

    <a class="ui primary button">Supprimer le compte</a>

{% endblock %}
