{% extends 'base.html.twig' %}

{% block title %}Modifier votre sortie{% endblock %}

{% block body %}
    <div class="row">
        <h1>Modifier votre sortie</h1>
        {{ form_start(sortieForm) }}
        {% if sortie.etat.id == 2 %}
            {{ form_row(sortieForm.etat, {'attr': {'checked': 'checked', 'disabled': 'disabled'}}) }}
        {% else %}
            {{ form_row(sortieForm.etat) }}
        {% endif %}
        {{ form_widget(sortieForm) }}
        <button class="ui primary button" type="submit">Enregistrer</button>
        {{ form_end(sortieForm) }}
    </div>
{% endblock %}
